# Security

This public artifact contains example values only. Production/HomeLab credentials, private keys, API tokens, PSKs, webhook values, and runtime state are not stored here. Do not apply example configuration directly to a production system.
