# Sanitization

This artifact is generated from a private Source of Truth using an explicit allowlist. Addresses use documentation ranges, site names are generic, identifiers are placeholders, and secrets are excluded. The generated tree is not a mirror and is one-way from Private to Public.
